import ILoginInputOptions from "@inrupt/solid-client-authn-core/dist/ILoginInputOptions";
export default interface ILoginOptions extends ILoginInputOptions {
    sessionId: string;
    prompt?: string;
    tokenType: "DPoP" | "Bearer";
    onNewRefreshToken?: (newToken: string) => unknown;
    inIframe?: boolean;
}

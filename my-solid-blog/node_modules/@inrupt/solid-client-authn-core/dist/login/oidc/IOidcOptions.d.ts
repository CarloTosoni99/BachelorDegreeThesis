import { IIssuerConfig } from "@inrupt/solid-client-authn-core/dist/login/oidc/IIssuerConfig";
import { IClient } from "@inrupt/solid-client-authn-core/dist/login/oidc/IClient";
export interface IOidcOptions {
    issuer: string;
    issuerConfiguration: IIssuerConfig;
    client: IClient;
    sessionId: string;
    refreshToken?: string;
    prompt?: string;
    dpop: boolean;
    redirectUrl: string;
    handleRedirect?: (url: string) => unknown;
    onNewRefreshToken?: (newToken: string) => unknown;
    inIframe?: boolean;
}
export default IOidcOptions;
